include ../input/input.pug

mixin dateDropdown(options)
    - const {heading, name} = options
    .dateDropdown
        +input({heading: heading, name: name, type: 'text', placeholder: 'ДД.ММ.ГГГГ', dateDropdown: true})
